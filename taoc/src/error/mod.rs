// ============================================================================
// error 模块入口
// ============================================================================

/// 编译器诊断错误类型定义
pub mod diagnostic;

// 重新导出核心类型，方便外部使用
pub use diagnostic::TaoError;

/// 统一的编译器结果类型别名
pub type TaoResult<T> = std::result::Result<T, TaoError>;
